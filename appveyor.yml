version: 0.1.{build}
image: Visual Studio 2017
init:
  - cmd: git config --global core.autocrlf true
before_build:
  - cmd: dotnet --version
  - cmd: dotnet restore . --verbosity m
build_script:
  - cmd: dotnet build
clone_depth: 1
deploy: off
