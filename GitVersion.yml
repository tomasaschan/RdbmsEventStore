mode: ContinuousDelivery
next-version: 0.6.0
branches:
  master:
    tag: beta
  (pull|pull\-requests|pr)[/-]:
    tag: alpha
  (?!(^master|^pull|^pr|^release|^hotfix|^support|^dev)):
    tag: feat
ignore:
  sha: []
